// "use strict"
// no.1 
// Display a random integer between 1 and 100 inclusive.
// for (let i=0; i<=100; i++){
//     let random = Math.floor(Math.random()*101)+1;
//     console.log(random)
// } 

// 053 
// Display a random fruit from a list of five fruits.
let question =("mango, apple, coconut, banana, peapple")
for (let k=0; k<question.length; k++){
    question = Math.ceil(Math.random());
    let question2=question[i]
} 
console.log(question2);

054 
// Randomly choose either heads or tails (“h” or “t”). Ask the user to make their choice. If their choice is the same as the randomly selected value, display the message 
// “You win”, otherwise display “Bad luck”. At the end, tell the user if the computer selected heads or tails.

